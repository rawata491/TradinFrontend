# Leave empty in development — Vite proxy forwards /api and /ws to the backend.
# Set to the absolute backend URL only when deploying without a reverse proxy.
VITE_API_BASE_URL=
VITE_WS_URL=ws://localhost:5173/ws
VITE_APP_NAME=Tradin
